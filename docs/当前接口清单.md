# 当前接口清单

由 FastAPI 实际路由导出；非按旧文档手工计数。

| 方法 | 路径 |
|---|---|
| GET | `/api/company/{scode}` |
| GET | `/api/company/{scode}/briefing` |
| GET | `/api/company/{scode}/chain` |
| GET | `/api/company/{scode}/graph` |
| GET | `/api/company/{scode}/supply-chain` |
| GET | `/api/country/{name}` |
| GET | `/api/evidence/{chunk_id}` |
| GET | `/api/health` |
| GET | `/api/meta` |
| GET | `/api/radar` |
| GET | `/api/segments` |
| GET | `/api/v1/analyses/{analysis_id}` |
| GET | `/api/v1/chat/sessions/{session_id}` |
| GET | `/api/v1/data-sources` |
| GET | `/api/v1/extensions` |
| GET | `/api/v1/me/capabilities` |
| GET | `/api/v1/me/preferences` |
| GET | `/api/v1/references/{ref_id}` |
| PATCH | `/api/v1/me/preferences` |
| POST | `/api/v1/briefings` |
| POST | `/api/v1/chat/requests/{request_id}/cancel` |
| POST | `/api/v1/chat/stream` |
| POST | `/api/v1/data-sources` |
| POST | `/api/v1/data-sources/{source_id}/activate` |
| POST | `/api/v1/data-sources/{source_id}/disable` |
| POST | `/api/v1/data-sources/{source_id}/validate` |
| POST | `/api/v1/extensions/{extension_id}/bindings/{region_id}` |
| POST | `/api/v1/feedback` |
| POST | `/api/v1/tools/{tool_name}` |

年份协议：详情/评分/推理链/简报/子图/供应链统一采用显式year；不指定则取该企业最新数据年度。指定年度不存在不回退。

/api/radar 返回 total（企业-年总数）和 items（展示条目）；/api/segments 支持 year、province、industry。

/api/meta 返回六维数量、国家/地区字典数量、全部主张/需求主张数量及有效/排除文本数量。

industry 使用现有七个细分名称；旧参数“光伏”兼容映射为光伏主链，“电气设备”指全部现有样本。
