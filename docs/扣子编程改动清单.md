# 扣子编程改动清单（v3 · 四项全部完成并经线上实测验收）

> 核实对象：`C:\Users\lenovo\Desktop\工行杯\project_20260925_202405\projects`
> 核实方式：逐文件读代码 + 与线上实测行为对照｜日期：2026-09-25（v3 更新）
>
> **结论：改动一 / 二 / 三 + 第 2 节的 N05 补取证据，四项已全部完成并上线。**
> 第 2 节所述的 N05 修复此前只落在扣子编程项目里，本地副本（`project_20260925_202405/projects`）
> 曾是未打补丁的旧快照；v3 已把本地副本同步为线上版本。

## 0. 线上实测验收（2026-09-25，v3 新增）

对线上网关发起名单类验收提问，实测通过：

```bash
curl -s -N -X POST 'https://shuzhi-lianhai-production.up.railway.app/api/v1/chat/stream' \
  -H 'Authorization: Bearer demo-token-region-a' -H 'Content-Type: application/json' \
  --data-binary '{"client_request_id":"verify","message":"江苏有哪些光伏企业值得关注？","page_context":{"year":2023}}'
```

| 观测项 | 结果 |
|---|---|
| SSE 事件序列 | `request_started → status×3 → answer_delta×4 → orbs_ready → analysis_ready → done` |
| 真实生效引擎 | `engine = langgraph` |
| **证据光球（本次修复的验收点）** | **`orbs`: evidence×3、product×2** —— 修复前该场景为 0 引用、0 证据球 |
| `bad_ref` 告警 | 无 |
| 回答是否采用三阶段口径 | 已采用（正文出现「出海落地期」「11 条筹备期信号、2 条落地期信号」） |
| 旧口径残留 | 无（旧五类标签 / 筹备层 / 落地层 / `stage_layer` 全部未出现） |

> 附带结论：N05 补取的证据随 `facts` 整体透传给 N07，因此**三阶段口径改造无需改动引擎侧任何提示词或节点代码**。

---

## 1. 核对结果：这三项**已完成**，不要再动

| 原改动 | 核实位置 | 证据 |
|---|---|---|
| **改动一** N07 提示词加引用规则 | `config/analyze_llm_cfg.json` → `sp` | 已含「**严禁自造引用**：不得把工具响应中的字段名、键名或序号当作引用 ID……杜绝生成 `ev:filter_explanation`、`ev:item:300827` 这类伪引用」与「引用格式约束：evidence 引用必须为 `ev:<chunk_id>:<claim_number>:<text_version>` 四段……**无法给出合法引用时，refs 留空数组，绝不编造**」✅ |
| **改动二** N05/N06 记录真实引用 | `nodes/fact_retrieval_node.py:61-62`<br>`nodes/product_retrieval_node.py:50-52`<br>`graphs/state.py:34` | N05：`seen = collect_refs(data)` → `seen_refs=sorted(seen)`；N06：`merged = set(state.seen_refs or []) \| collect_refs(data)`；状态里有 `seen_refs: Optional[List[str]]` ✅ |
| **改动三** N08 引用白名单 | `nodes/check_node.py:55-56`<br>`nodes/n08_ref_filter.py` | `ws_state = {"draft": draft, "seen_refs": set(state.seen_refs or [])}` → `n08_selfcheck(...)`；白名单模块逻辑完整（含**「半套光球清空」**——只给 product 球时整段清空，交回后端兜底）✅ |

**线上实测也印证了**：`[bad_ref]` 告警从 4 条降到 **0 条**，`ev:item:300827` 这类伪引用不再出现。
所以**不要重复改动这三处**。

---

## 2. 唯一必改项：N05 在「名单 / 对比」场景要补取可引用证据

### 2.1 为什么要改（代码级证据）

`src/graphs/nodes/fact_retrieval_node.py:30-50` 按意图**只调一个**工具：

```python
if intent == "search":    tool = "search_companies"     # ← 响应里没有任何引用字段
elif intent == "compare": tool = "compare_companies"    # ← 只返回 compare_ref，无 evidence 引用
else:                     tool = "get_company_context"  # ← 有 signal_refs[].evidence_ref ✅
```

后端工具侧的对应事实：

| 工具 | 响应里带引用吗 |
|---|---|
| `search_companies` | ❌ 只有 `items[]`（`scode`/`coname`/`province`/`window_label`/`score`…）与 `filter_explanation` |
| `compare_companies` | ⚠️ 只有 `compare_ref`，**没有** `ev:` 证据引用 |
| `get_company_context` | ✅ `signal_refs[].evidence_ref`（最多 6 条）、`rule_candidates[].product_ref`、`metric_refs.*` |

**后果链**（与线上实测逐条吻合）：

```
search/compare 场景 → 工具没给任何 ev: 
  → seen_refs 基本为空
  → 提示词又明确要求"拿不到就留空，绝不编造"
  → N07 输出 refs: []
  → N08 白名单过滤后仍是 []
  → orbs: [] → 后端 _default_orbs 没有引用可建
  → 页面上 0 个原文球、正文里没有「依据 N ↗」按钮
```

线上实测：`江苏有哪些光伏企业值得关注？` 与对比类问题 → **正文引用 0 条、证据球 0 个**；
而 `002860 这家公司值得关注什么？`（走 `get_company_context`）→ **引用 2 条、证据球 2 个**。
**差别就在这一个 if/elif 上。**

### 2.2 改法：整份替换 `src/graphs/nodes/fact_retrieval_node.py`

把该文件**整体替换**为下面内容（保留了 Coze 节点识别用的 `title/desc` docstring 与函数签名）：

```python
"""N05 事实检索节点 - 调用公网受控工具服务"""
import requests
from langchain_core.runnables import RunnableConfig
from langgraph.runtime import Runtime
from coze_coding_utils.runtime_ctx.context import Context
from graphs.state import FactRetrievalInput, FactRetrievalOutput
from graphs.nodes.n08_ref_filter import collect_refs

_DEFAULT_TOOLS_BASE = "https://shuzhi-lianhai-production.up.railway.app"
# 名单/对比类问题最多补取几家的上下文包（控制工具调用次数，建议 2—3）
_DISCUSS_LIMIT = 3


def _dedup_params(params: dict) -> dict:
    """剔除值为 None 的参数"""
    return {k: v for k, v in params.items() if v is not None}


def _call_tool(base_url: str, tool: str, params: dict, token: str):
    """调用受控工具，返回 (data, error)。error 非空表示失败，绝不伪造数据。"""
    url = f"{base_url}/api/v1/tools/{tool}"
    headers = {"X-Context-Token": token, "Content-Type": "application/json"}
    try:
        resp = requests.post(url, json=params, headers=headers, timeout=(5, 60))
        if resp.status_code != 200:
            text = resp.text if resp.text else f"HTTP {resp.status_code}"
            return None, {"error": f"工具调用失败：{text}",
                          "http_status": resp.status_code, "tool": tool}
        try:
            return resp.json(), None
        except Exception:  # noqa: BLE001
            return {"raw": resp.text}, None
    except Exception as e:  # noqa: BLE001
        return None, {"error": f"工具调用异常：{e}", "tool": tool}


def _scodes_from(data, limit: int) -> list:
    """从 search_companies / compare_companies 的响应里取出要讨论的企业代码。"""
    out, seen = [], set()
    if not isinstance(data, dict):
        return out
    for key in ("items", "companies"):
        for it in (data.get(key) or []):
            if not isinstance(it, dict):
                continue
            code = it.get("scode") or it.get("code")
            if not code:
                continue
            code = str(code).zfill(6)
            if code not in seen:
                seen.add(code)
                out.append(code)
    return out[:limit]


def fact_retrieval_node(state: FactRetrievalInput, config: RunnableConfig, runtime: Runtime[Context]) -> FactRetrievalOutput:
    """
    title: N05 事实检索
    desc: 按意图调用受控工具（search_companies / get_company_context / compare_companies）检索企业事实；名单与对比场景额外补取被讨论企业的可引用证据
    """
    ctx = runtime.context
    base_url = (state.tools_base_url or _DEFAULT_TOOLS_BASE).rstrip("/")
    token = state.context_token
    intent = state.intent or "explain"

    tool: str = ""
    params: dict = {}
    if intent == "search":
        tool = "search_companies"
        filters = state.filters if isinstance(state.filters, dict) else {}
        params = _dedup_params({
            "province": filters.get("province"),
            "industry": filters.get("industry"),
            "direction": filters.get("direction"),
            "year": state.year,
            "page_size": 5,
        })
    elif intent == "compare":
        tool = "compare_companies"
        scodes = [c.get("scode") for c in (state.companies or [])
                  if isinstance(c, dict) and c.get("scode")]
        params = _dedup_params({"scodes": scodes, "year": state.year})
    else:  # explain / plan / general（走具体企业）
        tool = "get_company_context"
        params = _dedup_params({
            "scode": state.scode,
            "year": state.year,
            "snapshot_id": state.data_snapshot if state.data_snapshot else None,
        })

    data, error = _call_tool(base_url, tool, params, token)
    if error:
        # 真实失败：记录真实错误状态，不伪造数据（由下游处理降级）
        return FactRetrievalOutput(tool_used=tool, facts=error)

    seen = collect_refs(data)

    # ---- 补取「被讨论企业」的可引用证据（search / compare 场景必需）----
    # 原因：search_companies 响应里没有任何引用字段、compare_companies 只有 compare_ref，
    # 若不再补取，N07 手里根本没有 ev: 可引用 → 正文没有依据按钮、页面没有原文球。
    if intent in ("search", "compare"):
        discussed = []
        for code in _scodes_from(data, _DISCUSS_LIMIT):
            one, err = _call_tool(
                base_url, "get_company_context",
                _dedup_params({"scode": code, "year": state.year}), token)
            if err or not isinstance(one, dict):
                continue
            discussed.append(one)
            seen |= collect_refs(one)
        if discussed:
            data = dict(data)
            # 塞进 facts 即可：N07 的 up 模板是 {{facts}}，整个 JSON 会原样给到模型
            data["discussed_companies"] = discussed
            data["discussed_note"] = (
                "discussed_companies 为名单/对比中被讨论企业的上下文包，"
                "其中 signal_refs[].evidence_ref 是可引用的原文证据，请优先引用。")

    return FactRetrievalOutput(tool_used=tool, facts=data, seen_refs=sorted(seen))
```

### 2.3 三个要点

1. **不用改 `up` 模板。** `analysis_node.py:32` 是 `"facts": json.dumps(state.facts, ...)`，
   而 `config/analyze_llm_cfg.json` 的 `up` 是 `事实包：{{facts}}` —— 只要把补充结果
   **塞进 `facts`**（键名 `discussed_companies`），模型自然看得到 `signal_refs[].evidence_ref`。
2. **不用改 `state.py`。** 复用现成的 `facts` / `seen_refs` 字段即可，不新增状态字段。
3. **工具调用次数**：名单/对比问题从 1 次变成最多 4 次。若担心超出 `max_tool_calls`，
   把 `_DISCUSS_LIMIT` 调成 `2`。

### 2.4 可选：给提示词再加一句（非必需）

在 `config/analyze_llm_cfg.json` 的 `sp` 末尾追加：

```text
- 当事实包中出现 discussed_companies 时，回答中列举或对比这些企业，
  每家至少引用一条其 signal_refs[].evidence_ref（原样摘抄，不得改写）。
```

> 加不加都行——**关键是有引用可抄**；有了 `discussed_companies`，即使不加这句，
> 模型也能在事实包里找到 `ev:`。

---

## 3. 顺带清理（可选，不影响功能）

### 3.1 `src/graphs/nodes/ref_filter.py` 是**死代码**

已确认**没有任何文件 import 它**（全项目 grep `ref_filter` 只命中它自己）。
`check_node.py:8` 用的是 `n08_ref_filter`。两份实现并存容易日后误改，建议**删除该文件**。

### 3.2 `check_node.py:58-59` 的 `valid` 语义偏松

```python
warnings = clean_draft.get("warnings") or []
issues.extend(w for w in warnings if isinstance(w, str))
valid = len(issues) == 0
```
它把草稿里**所有** warnings（包括模型自己写的口径告警，如"placeholder 类产品卡片仅为参考…"）
都算成 `issues`，于是 `valid` 几乎恒为 `False`。
**影响面**：`output_node` 不使用 `valid`（只输出 `clean_draft`），所以**当前不影响结果**——
但语义失真，建议只把自检自身产生的告警计入：

```python
warnings = [w for w in (clean_draft.get("warnings") or [])
            if isinstance(w, str) and "已剔除" in w]
issues.extend(warnings)
```

---

## 4. 部署与验证

### 4.1 部署
工作流开发页右上角 → **部署** → 选最新开发版本 → 开始部署。
域名不变（`kmj3bsz4kj.coze.site`）→ **后端环境变量不用改**。

### 4.2 验证（两类问题各测一次，缺一不可）

| 测试问题 | 验证的是 | 期望 |
|---|---|---|
| `002860 这家公司值得关注什么？` | 单企业路径（本来就通） | `answer_blocks[].refs` 非空，有 evidence 球 |
| **`江苏有哪些光伏企业值得关注？`** | **本次改动的验收点** | **`refs` 非空、`orbs` 里有 `kind:"evidence"`** |

对比类也要测：`上能电气和中信博相比，谁更适合先谈跨境资金池？为什么？`

```bash
# 1) 提问，从 SSE 的 analysis_ready 事件里取 analysis_id
curl -s -X POST 'https://shuzhi-lianhai-production.up.railway.app/api/v1/chat/stream' \
  -H 'Authorization: Bearer demo-token-region-a' -H 'Content-Type: application/json' \
  -d '{"client_request_id":"v1","message":"江苏有哪些光伏企业值得关注？","page_context":{"year":2023}}'
# 2) 查落库结果
curl -s 'https://shuzhi-lianhai-production.up.railway.app/api/v1/analyses/<analysis_id>' \
  -H 'Authorization: Bearer demo-token-region-a'
```

**通过标准**
- `draft.answer_blocks[].refs` 非空，形如 `ev:v21_cXXXXXXXX:<数字>:<哈希>`；
- `draft.orbs` 里至少 1 个 `"kind": "evidence"`；
- `draft.warnings` 里没有 `[bad_ref] …已移除`。

> 后端还有一层兜底（已上线）：当页面上有具体企业时，即使引擎没给引用，后端也会补挂
> 带「（后端补充）」字样的证据球。但**名单类问题没有具体企业，兜底无从挂起**——只能靠本节改动。

---

## 5. 核对过的文件（备查）

| 文件 | 结论 |
|---|---|
| `src/graphs/graph.py` | 链路 N05→N06→N07→N08→N09 正确，无需改 |
| `src/graphs/state.py` | `seen_refs` 已加；`facts`/`seen_refs` 可直接复用 |
| `src/graphs/nodes/fact_retrieval_node.py` | ⚠️ **需替换**（第 2.2 节） |
| `src/graphs/nodes/product_retrieval_node.py` | ✅ 已正确累积 seen_refs |
| `src/graphs/nodes/analysis_node.py` | ✅ `up` 注入整个 facts，无需改模板 |
| `src/graphs/nodes/check_node.py` | ✅ 已接白名单；`valid` 语义可选优化 |
| `src/graphs/nodes/n08_ref_filter.py` | ✅ 逻辑完整（含半套光球清空） |
| `src/graphs/nodes/ref_filter.py` | 🗑️ 死代码，建议删除 |
| `src/graphs/nodes/output_node.py` | ✅ 输出 `clean_draft`，正确 |
| `config/analyze_llm_cfg.json` | ✅ 引用规则已写入；可选再加 `discussed_companies` 一句 |
